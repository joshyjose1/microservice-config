name: Hello World Workflow
run-name: ${{ github.actor }} is running the Hello World action 🚀
on: [push]

jobs:
  say-hello-job:
    runs-on: ubuntu-latest
    steps:
      - name: Greet the world
        run: echo "🎉 Hello, world! This is my first GitHub Action."
      - name: Display current repository
        run: echo "🔎 This workflow is running in the ${{ github.repository }} repository."
